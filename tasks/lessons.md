# 教訓(ユーザーからの修正を受けて追記)

## 2026-08-23
- **間違いを指摘されたら1行目で認める。** 説明や経緯を先に並べると「有耶無耶にしている」と受け取られる。構成は「認める → 何が間違いか1行 → 正しい答え」。
- **前提を確認せずに書かない。** 「iPhoneで http://<MacのIP>:8931」と書いたが、地下では同一Wi-Fiにいない。`git remote` と GitHub Pages(https://teacoffewater.github.io/umeda-dungeon/)を先に確認していれば最初から正しい案内ができた。
- **回答は端的に。** 結論→理由(箇条書き3つ以内)。仕様書は60行以内、詳細は実装計画へ。「中学生でもわかるように」と言われたら、専門用語を使わず手順で書く。
- **用語や方針を変えたいときは先に提案し、了解を取ってから文書に反映する。** 「px」→「メートル」の言い換えを、提案はしたが文書を変える前に一言確認すべきだった(しれっと直したと誤解された)。
- **質問には聞かれたことだけ答える。** 「1px=1mで足りる?」に対して仕様の話を混ぜて答え、「答えになっていない」と言われた。意味が複数あるなら、それぞれ1行ずつ。

## 2026-08-24
- **過去の経緯を聞かれたら、答える前にセッションログ(`~/.claude/projects/<project>/*.jsonl`)を確認する。** 記憶がない状態で推測で「勝手にやった」と認め、事実(ユーザーの指摘を受けた変更)と食い違った。

## 2026-08-25 詳細地図「トレースできてる？」の指摘
- **教訓**: 「フロアガイドをトレース」と合意したのに、区画と店位置だけ抜いて床はOSM帯を流用した。部分実装を「できた」と見せない。層(床・区画・店)ごとに「どこまでが約束のデータ源か」を明示する
- **技術メモ**: フロアガイドPDFの通路は白ポリゴンではなく「区画列の隙間」。床外形は 区画∪白片 のモルフォロジー閉包(±8m)で再構成できる。飾り帯(路線図)は 40m超×8m未満の細長判定で除去。分断成分は最近接点を幅8m帯で接続
- **検証メモ**: Three.jsのRaycasterは非表示オブジェクトも拾う(visible連鎖を自前で確認する)。詳細モードの範囲判定は外接ボックス不可(V字のくぼみに他施設が入る)→床形状との距離で判定

## 2026-08-25 詳細地図=ガイドそのまま(位置合わせ不要)の理解ミス
- **教訓**: 「二層構造」の核心は座標系の分離だった。詳細地図を実座標に無理に位置合わせして歪ませたのは、ユーザーの「広域と詳細は別物・シームレス不要」を理解していなかったため。ローカルに正しい地図(紙のフロアガイド)はローカル座標のまま使うのが正解で、案内もその中で完結すれば間違えようがない

## 2026-08-25 「本当にトレースした？」再指摘(床がガタガタ)
- **教訓**: 「トレース」と言うなら原本に描かれている線を使う。区画の集合から床を復元(閉包)したのは近似であり、印刷物のきれいな外形線(灰色ストローク・全周1本)がPDF内に存在した。まず「原本にその形の線が無いか」を探すこと
- **技術メモ**: pymupdf get_drawings で type='s' の長大ストローク(items>100)がモール外形線。南側(mikke接続部)だけ開いており、単純に閉じるとmikke一帯が落ちる→外に落ちた区画群を閉包して結合

## 2026-09-03 詳細地図が真っ暗になる(「何も表示されない時がある」)
- **原因**: 広域⇔詳細はカメラを900msかけて飛ばすが、`pointerdown` で `camAnim = null` していたため、遷移中に画面に触れると移動が中断し、**座標系の間の何も無い空間にカメラが取り残されていた**(詳細地図は広域から x+1600 離れた専用エリアにある)。せっかちなタップ・二度タップで再現する
- **修正**: 広域⇔詳細の移動には `hard: true` を付け、pointerdown では中断せず `finishCamAnim()` で行き先へ即着地させる。加えて詳細モード中は注視点を地図の実体から一定距離以内に引き戻す(パンで地図を画面外へ追い出せないように)
- **技術メモ**: 引き戻しの基準に外接ボックスは使えない(ホワイティはV字で、ボックスの角には地図が無い)。床の外形点と区画重心を `detailAnchors` に集めて最近傍距離で判定する。許容距離は「いま見えている範囲」(`dist * tan(fov/2)` と aspect から算出)に比例させる
- **検証メモ**: この手の「時々おかしい」はカメラ座標を実測すると一発。`__dbg.camera.position` と詳細地図の外接ボックスを並べて見る。Playwright は遷移中(200ms後)に `mouse.down/up` を撃てば再現できる

## 2026-09-03 集約ドットのタップで出発地の名前が空欄になる
- **原因**: 広域のモール集約ドットのタップ先(`MERGED_DOTS.near`)が、ホワイティの10エリアすべてで `J()` の分岐点ノード(`name: ''`・zoneも持たない)だった。ルート探索自体は正常で、壊れるのは名前表示だけ(`現在地: （中枢層）` /「」を出発)
- **修正**: ホワイティのドットは端点にせず、床タップと同じ「詳細地図への入口」にした。店を選ぶのは詳細地図の管轄という二層構造の方針どおり。三番街・イーマは詳細地図が無いので従来どおり館ノードを選択する
- **技術メモ**: ドットを `nodeMeshes` から外すだけでは無反応になる(床レイキャストは `floorGroups.B1` の全メッシュを見るのでドットが床の手前で当たる)。ドット側に `userData.zone` を持たせて床と同じ判定に乗せる
- **調べ方**: 「どのタップで壊れるか」は入口を全部数えると早い。検索候補は `NODES.filter(n => n.type !== 'junction')` で名前なしを除外済み、タップ対象は `nodeMeshes.push` の4箇所だけ、と絞れた

## 2026-09-03 傾斜の記録(坂・階段)を実測込みにした
- **技術メモ**: `.sv-row { display: flex }` が `[hidden]` のUA既定に勝つため、調査モードのフォームの行は**一度も隠れていなかった**（「どこへ」「出口番号」「坂」が全種別で出ていた）。`.sv-row[hidden] { display: none }` を足して解消。`hidden` 属性で出し分けるときは、その要素に `display` を当てているCSSが無いか確認する
- **設計メモ**: 斜度は保存しない。2点の `px`・`px2` と高低差から再計算できる派生値で、`convert_survey.py` が座標フレームを変換すると保存済みの値は古くなるため。表示だけ都度計算する
- **現地メモ**: 階段は段数を数えるのが一番速くて正確なので、段数→高低差(段数×0.17m)を自動見積り。坂は2点タップで水平距離が確定するので、高低差を入れれば斜度%が出る

## 2026-09-03 現地の館内案内板の写真から詳細地図を起こす(堂島アバンザ)
- **技術メモ**: 案内板は「区画=オレンジ / 通路=白 / 駐車場等=ベージュ」と色分けされているので、色で3値化 → **収縮(3〜4セル)で細い線を落としてから連結成分**に分けると、隣接区画(⑧+③、②⑤⑥⑦)と図面の枠線がきれいに切れる。膨張で元の大きさに戻してポリゴン化(`tools/extract_avanza_board.py`)
- **落とし穴**: 目視で読んだ区画番号の座標(NUM)は「読んだときの画像サイズ」に依存する。素材を縮小して保存し直したら照合のスケールがずれて別の区画に付いた(店位置が最大54mずれた)。座標を読んだ画像サイズをコメントに残し、素材と比較して換算する
- **縮尺**: ガイド座標系は実座標と合わせないが、距離・所要時間に効くので m 換算はする。建物の東西80m/南北60m(OSM外形)を写真の幅・高さに当てた(写真の縦方向は撮影角度で縮むので縦横別々の倍率)
- **接続部**: ドーチカとの接続3本は階段で、案内板では白でないので色抽出に乗らない。手で矩形を置いて歩行可能にした(位置は現地の2点タップで補正する前提)

## 2026-09-03 阪急三番街の詳細地図(公式フロア図PNG + 現地の案内板)
- **設計メモ**: 施設が館×階に分かれる場合は、DETAIL_MAPS のキーを館×階(`sanban_n_b1` 等)にし、`zone: 'sanban'` を共有・`areas`(shops.js のエリアID)で店を振り分ける。`floor` で描画高さ(B1F=浅層)を変える。床タップは「同じゾーン・同じ階の詳細地図のうち、タップ位置に近い方」
- **技術メモ**: 区画番号の対応表は区画ポリゴンの index ではなく**重心(px)**で持つ。抽出の閾値を変えると index がずれて全部付け直しになった(`--convert` で移行)
- **技術メモ**: 公式フロア図は階ごとに描き方が違う(通路が白 / 明るい灰 / 桃色地に点柄)。フロアごとに通路の検出モード(`CORRIDOR_MODE`)を持つ。透過PNGは白に合成してから分類(透明色の緑が「区画」に化けた)。図の外側の余白・凡例・方位記号は `cuts`(px矩形)で床から除く
- **データメモ**: shops.js の三番街の店は**区画番号の昇順**で並んでいる(欠番はスキップ)。ただし店の入替(北館B2F 14 舎鈴)があるので、番号→店名は現地の案内板の一覧を正とし `names` に明示する

## 2026-09-03 三番街南館の集約ドットをタップしても詳細地図に入らない
- **原因**: 館ノード(`P()`)の当たり判定は半径18mの**透明シリンダー**で、南館は館ノード(955,682)と集約ドット(973,683)が18m以内。タップは床より手前で館ノードに当たり、出発地選択(名前あり)になっていた。北館は館ノードが館の北西端(925,587)にあり、ドット中心とは離れていたので偶然動いていた
- **修正**: 詳細地図を持つ館では館ノードのタップも床タップと同じ「詳細地図への入口」にした(`detailKeyForSpotHit`)。館ノードを端点にしたければ検索から選べる
- **調べ方**: `__dbg.nodeMeshes` / `__dbg.floorGroups` を晒して、pointerup と同じ順(店・スポット → 床)でレイキャストを再現する(`rebuild/sanban_dbg2.js`)。scene 全体の交差だけ見ても「何が判定を横取りしたか」は分からない

## 2026-09-03 アバンザの詳細地図にドーチカの通路が入っていた(接続は3ヶ所)
- **原因**: 館内案内板は周辺の公共通路(ドーチカ本線と南西の枝)も館内と同じ白で描く。白成分を「区画に接する or 大きい」で拾ったため、西側を南北に走るドーチカ本線が丸ごとアバンザの床になった。接続部の階段3本も、うち2本の位置を読み違えていた(格子付きの拡大画像で座標を読まずに置いた)
- **修正**: 縮小pxでドーチカの範囲(通路+ベージュ帯+その白い縁取り)を多角形で定義し、白成分から**幾何的に差し引く**(重心判定では、南西の枝が館内の南側通路と同じ成分になって残る)。階段3本(北=北サンクンガーデン通路、中=南サンクンガーデンへの曲線階段の手前、南=車椅子EV隣)は格子画像で読んだ座標に置き直し、館内の曲線階段(斜線帯で白に乗らない)を歩行帯として足した
- **広域側**: ドーチカ⇔アバンザの接続は南のC-84(avanza→j_avz)1本だけだったので、北・中の階段を J ノード+エッジで追加(位置は案内板の比率からの仮値、現地の2点タップで補正)。ドーチカ側のノードは @region dotica の生成エッジを割って挟んでいるので、import_region で再取り込みしたら手で戻す
- **教訓**: 案内板から床を起こすときは「施設の床」と「周辺の公共通路」を最初に切り分ける(白=歩ける、ではない)。座標を目視で読むときは `tools/_debug` に20px格子を焼いた拡大画像を作ってから読む(拡大なしの読みは20〜40px単位でずれた)

## 2026-09-03 阪急三番街 北館/南館の分断(B1Fは直結なし、B2Fは連絡通路2本)
- **指摘**: B1Fは南北でつながっていない。B2Fは小さい道2本でつながっている。広域地図もそうなるように
- **原因**: 広域は北館/南館を1枚の床にしていた(S1は斜めの細い切れ目だけ、B1は貫通)。通路グラフも B2F は幅10mの1本で直結
- **修正**: 市道で床を分断(S1=全断、B1=幅4mの連絡通路2本だけ残す)。通路グラフは B2F の連絡通路2本を J ノード+エッジにし、館の格子(rect)から最寄り格子点で接続(shops.js の `near[1..]`)。B1F は直結エッジ無し=B2F 経由の案内になる
- **市道の位置は OSM が正**: 建物を東西に貫く `highway=service tunnel=building_passage`(way 682682107)の東西区間 y≈565〜575。当初「紀伊國屋(1F)=北館」と思い込んで y≈621 に置いたが、OSM の紀伊國屋(362616258)はこの道路の**南**なので南館側。北館ノード・ESC(y≈582)は道路の南にあったので北館内(y≈548)へ移し、ビッグマンは南館ノードにつなぎ替えた
- **検証で気づく仕組み**: validate_map の「S1の島に昇降設備がなく未登録」が、分断した北館の床に ESC が無い(=ESC の位置が南館側)ことを教えてくれた。床を分断するときはノード・VERTICALS がどちら側に落ちるかを必ず見る
- **技術メモ**: 分断は BUILDING_PLATES の差し引きだけでは足りない(通路帯やOSM中心線が後から埋める。1Fの横丁 `level=0` が S1/B1 に紛れていた)。ゾーン面の合成後に `SANBAN_ROAD` を差し引く
- **未確定**: 連絡通路2本の位置は案内板の比率(東端から22%・西端から26%)。館の rect の実寸(70×42 / 75.3×49.9)も案内板の縦横比からの推定で、現地確認待ち

## 2026-09-03 ドーチカの詳細地図(公式フロアマップの画像から)
- **入力の扱い**: 公式マップはチャットに画像として届いたが、アップロード先(/root/.claude/uploads)にファイルが無く、ピクセル処理ができなかった。画像を目で読んで「区画の並び(側・x範囲px)」の表(`tools/dotica_spec_from_map.py`)に起こし、`trace_linear.py`(軸+並び)で区画ポリゴン化した。位置精度は px 読みの ±10px(≈1.5m)。ファイルで受け取れたら色抽出に置き換える
- **向き**: 公式マップは N が左(上が東・右が南)。北を上に回転して置いた(回転は歪みではないので、ガイド座標系の方針=形を変えない、には反しない)
- **縮尺**: 通路全長(C60 の端〜南端 ≈1855px)を広域の通路長(OSM 中心線 ≈300m)に当てて 0.155m/px。南側(x>1315)は図が右上がりに折れているので軸も東へ折る(15°)
- **trace_linear の拡張**: `at`(起点からの絶対距離)・`off`(通路の縁からの後退。手前に浅い区画がある奥の区画)・`walk: true`(出入口の通路は区画でなく歩ける床)。奥行きが違う店の重なり(plus on/レオニダスの奥に案内所・東京油組、ちぐさの奥にオズ)はこれで表せる
- **広域側**: 詳細地図を持つ施設の方針どおり、shops.js の dotica を北・中・南の3つの `merged` rect(通路上・幅10m)に分け、集約ドット=詳細地図の入口にした。53店の名前は公式マップと同じ(2026-07 採録)なので実位置は全店に付いた

## 2026-09-03 堂島ふらっと(近鉄堂島ビルB1F)を接続ビルとして追加
- **発見**: ドーチカ C83 の階段の先(近鉄堂島ビル地下)に「堂島ふらっと」(クリニック・研修センター・理容室など16区画)。現地のユーザーの発見
- **追加の型(接続ビルの追加はこの5点)**: ①ZONES に色とラベル ②館ノード P + ドーチカ側ノードからの接続エッジ ③gen_polys の FACILITY_BLD / MASK_HOLES(接続帯の穴) / BUILDING_PLATES / ORDER(**ORDER に無いゾーンは床が出力されない**) ④validate_map の FAC と INTRUSION_WHITELIST ⑤shops.js の merged rect(入口ドット)+ detail_maps.js
- **抽出メモ**: 案内板の番号の白丸が緑区画の**穴**になる。連結成分をそのまま使うと `contains`/距離判定が番号の位置で外れる(=番号が全部付かない)。外周だけ(`Polygon(g.exterior)`)を採ってから照合する
- **未確定**: C83 の階段(ドーチカ側 ESC)の段数・高低差。板の南側の地上出口3つと北東の四つ橋筋出口は地下完結の方針で入れていない

## 2026-09-03 関電不動産西梅田ビル地下街(B1F/B2F)を接続ビルとして追加
- **資料**: B2 案内板(線画・区画4つ)とテナント板(B1: 8店 / B2: 4店)。動画は案内板を写した0.3秒の静止クリップで追加情報なし(ffmpeg は無いが `pip install opencv-python-headless` でフレーム抽出できる)
- **向きの決め方**: 案内板の「堂島地下センター」出口は板の下側だが、実際の接続はビルの北東角(ドーチカ C61 は通路の北寄り、ビルの東面のすぐ隣)。線画は縮尺も向きも書いていないので、**接続点の実位置(OSM外形+ドーチカの出口番号)から板の向きを逆算**した(南が上=180°回転)。現地確認待ち
- **階層**: ビルの B2F がドーチカと同じ高さ(=中枢層 B1)、B1F はその上(=浅層 S1)。館内階段は VERTICALS の stairs(a=上, b=下)。B1F は案内板が無いので詳細地図なし=ドットは館ノード選択(DETAIL_MAPS に無い階はそのまま従来動作になる)
- **線画の起こし方**: 色抽出はせず、表示px で読んだ角を `tools/kanden_b2_from_board.py` に手置き → gen_detail。区画が数個ならこれが最速

## 2026-09-03 調査モードで詳細地図にも記録できるようにした
- **仕組み**: 調査バーの「詳細地図」で施設を選んで開くと、タップはその施設の専用グループ(ガイド座標系)にレイキャストし、`g2w` の逆変換でガイド座標(m)に直して記録する。記録には `frame: 'guide:<施設ID>'` と `detail` を持たせ、広域の記録(frame 無し=書き出し全体の metric-v1)と区別する。マーカーも施設の専用グループに置くので、詳細地図を開いているときだけ見える
- **用途**: 施設の中の位置合わせ(アバンザの階段3本、ドーチカの出口とアバンザの対応、三番街の連絡通路2本など)。反映先は `tools/data/detail/<施設>.json` の階段・出入口・walks。広域の取り込み(import_region / convert_survey)はこの記録を無視する(convert_survey は書き出し全体の frame しか見ない)
- **技術メモ**: 調査モードではタップが全部記録に取られるので、詳細地図へは床タップでなくバーの選択で入る。Raycaster は非表示のマーカーも拾うため、マーカー削除の判定は親まで表示中のものに絞る。2点ものの水平距離はガイド座標もメートルなのでそのまま出る

## 2026-09-03 資料が店リストしか無い階の詳細地図(関電不動産西梅田ビル B1F)
- **やり方**: 区画図が無い階は「仮配置」で詳細地図を作る。外形は同じ建物の他階(OSM外形)、テナントは通路の両側に資料の順で等間隔。名前に「(仮配置)」を入れて、案内板か現地記録で置き換える前提にする(`tools/kanden_b1_provisional.py`)
- **理由**: 詳細地図が無い階はドットが館ノード選択のままで、同じ建物の中で階ごとに挙動が変わって分かりにくい。仮でも詳細地図があれば調査モードでその階に記録でき、店同士の案内も引ける

## 2026-09-03 吹き抜け(地下だが地上が見える空間)の記録と表示
- **設計**: 吹き抜けは通路にしない(ルート探索・床には影響しない)。LANDMARKS の `kind: 'atrium'` + `rect`(対角2点)で持つ。**本番(通常モード)では描かない**(ユーザー指示。「表示だけ」は打ち間違いだった)。調査モード(?survey=1)でだけ、床から地上(GROUND_Y)まで抜ける半透明の柱+床の板+「☀ 名前」ラベルで描いて現地で見比べる。案内文には目印(曲がり角の「○○の所で右へ」)として乗る(nearestLandmarkTo は範囲の中心で判定)。`frame: 'guide:<施設ID>'` なら詳細地図の専用グループに描く(案内文には使わない)
- **調査モード**: 種別「吹き抜け」は2点タップ(対角)。`TYPES.rect` で面ものと線ものを区別し、マーカーは矩形の枠で描く
- **落とし穴**: 詳細地図のラベルは `D.labels` に入れると `buildDetailLabels` の「作成済み」判定(length>0)に引っかかり店ラベルが作られなくなる。店以外のラベルは `D.extraLabels` に分けて enterDetail/exitDetail で出し分ける
- **初期データ**: アバンザの北/南サンクンガーデンを案内板から入れた(広域は建物外形からの概略、詳細は案内板の縮尺)。現地の記録で置き換える

## 2026-09-03 堂島ふらっとの吹き抜け(サンクンガーデン)の見落とし
- **指摘**: ドーチカからの階段と店の入口の間に吹き抜けがある。案内板の右下(木2本+地上への曲線階段)がそれ
- **原因**: 色抽出で木の緑の丸(600〜800px²)が「区画」になり、広場から館内へ入る通路口を塞いでいた(詳細地図内の経路は別ルートで通っていたので気づかなかった)。吹き抜け自体はどこにも記録していなかった
- **修正**: 緑区画の最小面積を1500px²にして木を落とす。吹き抜けは landmarks.js の atrium で広域(現地記録の階段の西〜南西、概略)と詳細(案内板の縮尺)に登録。歩ける床のまま(地下完結の経路の一部)
- **教訓**: 案内板の「木」「曲線階段」「地上」の記号はサンクンガーデン(吹き抜け)の印。接続ビルを起こすときは、接続階段の先に屋外の広場が挟まっていないかを先に見る

## 2026-09-03 高低差モデル(層の既定の深さ + 実測で上下)
- **設計**: 層は「既定の深さ」(浅層3m/中枢層6m/深層9m)として扱い、施設の実測 `ZONES.depthM` と点の実測 `NODE_DEPTH` があれば `yOfDepth(d) = FLOOR_Y.B1 + (6−d)×22` で床・ノードを上下させる。縦の縮尺は層間隔(3m=66)と同じにして、層の高さと施設の深さが一つの尺度に乗るようにした(アバンザ2.4mは浅層の既定3mより浅いので浅層より高く出る。それが実測どおり)
- **描画**: 床プレート・広場・集約ドット・目印・吹き抜け・昇降設備・ルート線・ゾーンラベルは全て `yOfZone`/`n.y` を見る。両端の深さが違うエッジは gen_polys が床帯から外し(NODE_DEPTH を main.js から正規表現で読む)、main.js の addCorridor が両端を結ぶ斜めの箱として描く=階段のランプ
- **距離**: 経路コストと案内の距離は水平距離(`hdist`)に変えた。以前は posOf の3D距離だったので、層またぎの通常エッジに誇張した高さ(66=132m相当)が乗っていた。同一層内の階段は元から乗らない
- **注意**: 分岐点(J)は zone を持たないので、つながる通路の zone から深さを推定する。階段の途中の点は NODE_DEPTH に必ず書く(書かないと両端が同じ高さになりランプが出ない)

## 2026-09-03 LINKS UMEDA(リンクス梅田) B1F の詳細地図(現地の案内板から)
- **写真の向き**: 案内板が縦置きで、写真は90°回転+テナント一覧は上下逆。先に PIL で北を上(rotate -90)/180° に直してから読む。向きの根拠は「御堂筋線 ENT」が南側、ヨドバシ本体が西側
- **色分け**: 区画=茶(Harves・マクド等)/黄(オイシイもの横丁 20〜41)/オリーブ(16〜19)、ヨドバシ売場=灰。通路は背景と同じ薄いベージュなので色では取れず、**外形(目視の多角形)の内側−区画−暗い記号** を歩ける床にした
- **番号の文字が区画を割る**: 黄区画の黒い番号(太さ数px)で成分が欠片に割れた。closing(膨張→収縮)は区画の境の白線まで埋めて隣同士が合体する。**暗い画素のうち区画色に隣接するものだけを区画に足す**(`mask | (dark & dilate(mask, 5))`)と、文字は埋まり白線は残る
- **店名**: 案内板の表記と shops.js の表記が違う店(鈴音・函太郎・しんちゃん・桐麺)は、抽出側の NAMES に shops.js の表記を書いて名前照合を通す(板の表記はコメントで残す)

## 2026-09-03 ドーチカの広域の形を地下近辺案内板から起こし直した
- **指摘**: ドージマ地下センターがいびつすぎる。特に他の施設との接続が現実に即していない。板(南端 C93 の「地下近辺案内」)が正確なのでそれを当てて直す
- **原因**: 広域のドーチカは OSM 中心線(1本の折れ線)に幅12mの帯を被せただけで、枝(C61/C72/C80/C83/C84 の通路)と接続ビルの位置は案内板の比率や現地タップからの推定だった
- **やり方(施設の広域の形を案内板から起こす型)**: ①板の建物(青=地下接続ビル・灰・水色)を色で成分にし、OSM のビル外形と対応付ける ②ホモグラフィを **ICP(板の外形の点→OSM 外形の辺の距離、12m で頭打ち)** で当てる。出口番号(OSM の出入口ノード)は地上の位置なので初期値にだけ使う(残差 5〜10m あった) ③板の床(桃=区画+白=通路)を H で metric に写して床ポリゴンに、本線の軸は白い通路を上から追跡(前の行 ±22px の白の中央値。通路上の文字で切れた行は飛ばす) ④枝と接続ビルのノードは板の通路の位置を px で指定して H で変換
- **見落としかけた点**: 通路の東(C72〜C84 の間)の桃色部分はドーチカと同じ色だが、7段上がった広場と17段上がったアバンザ B1F の面(サンクンガーデン)で高さが違う。板の色=施設ではない。avanza ゾーンの床(FACILITY_BLD の西への拡張)にして、ドーチカとの境は板の区画帯の東縁(x=748px)で共有する。ビル外形の拡張は `plate()` と同じく +3.5 で通路側に重ねないと 3m の隙間が出る(通路帯はビル壁+3px で止まるため)
- **現地タップとの食い違い**: 2点タップの記録(中の階段 y≈1507、C83 の階段 x≈693)は旧地図の通路を基準にタップしたものなので、板の通路(y≈1511〜1516、x≈666)へ置き直した。記録 JSON の `_applied` に置き直しを書く。タップ記録は「段数・向き・順序」を正とし、位置は資料(案内板)を正とする
- **地上出口専用の枝は入れない**: C57 の通路(板の上端で切れている)は除外。C60/C69/C92/C93 の階段は床の外形に含まれる小さな出っ張りのみ
- **技術メモ**: scipy は無い。8パラメータのホモグラフィは numpy でガウス・ニュートン(数値ヤコビアン+小さい減衰)で十分(60 反復 15 秒)。板の床は通路上の文字・記号(現在地・トイレ)で割れるのでクロージング(dilate/erode 4px)してから最大成分を取る
- **追記(同日)**: 最初の写真は板の上端が切れていた。切れていない写真をもらって当て直したら縮尺が縦横とも 0.333 m/px(板のスケールバー 150m と一致)になった。**縮尺の初期値はスケールバーから取る**。出口だけからアフィンを解くと OSM の出入口(地上の位置)のずれで横の縮尺が 0.26 に化け、ICP がその局所解から抜けなかった(建物の残差は平均 6m でそれらしく見えるので気づきにくい)。板の建物1つの px 幅×縮尺が OSM の幅と合うかを必ず一度は手で確かめる
- **技術メモ**: `extract_dotica_board.py --apply` で main.js の該当ノードの座標を書き換える(P/J の行を正規表現で置換)。集約ドット(shops.js の rect)の x は本線の軸から出力するので手で写す

## 2026-09-03 詳細地図に他施設への接続の矢印・ラベルを付けた
- **指摘**: 詳細地図では他の施設への接続の通路がどこか分からない。矢印と「堂島アバンザへ」のような文字を入れて分かりやすく
- **設計**: 詳細地図はガイドの形そのままで周りを描かないので、接続は `DETAIL_MAPS[key].links`(ガイド座標の位置・向き・ラベル)で持ち、専用グループに金色の矢印(押し出した三角)と CSS2D ラベル(`.detail-link-label`、`D.extraLabels` で開閉に連動)を置く。位置は各施設の資料の階段・通路(tools/data/detail/<施設>.json の walks/_stairs)から
- **ホワイティの接続位置の出し方**: 広域の通路グラフでホワイティの縁にあるノード(他ゾーンとの境・館ノード)を、whity_2016.json の blocks(m↔g)から解いたアフィンでガイド座標に写し、床の外周に最も近い点へ寄せる(通路の先はガイドの床に無いので、そのまま置くと床の外に浮く)。向きは寄せた点→元の位置
- **落とし穴**: 一本道の施設(ドーチカ)は南端で軸が東へ折れているので、`x=0` のつもりで置くと床の外に浮く。置く前に外形の水平スライスで通路の x 範囲を確かめる
- **接続ビルの追加(資料が見聞だけ)**: 紀陽ビル B1F は「入って左が福永診療所、右が堂島デンタルクリニック」だけなので、OSM 外形+東面中央の入口+通路の左右に区画、の仮配置(tools/kiyo_b1_provisional.py)。名前に「(仮配置)」を入れ、置き換え前提にする(関電 B1F と同じ型)

## 2026-09-03 詳細地図の店名ラベルが多すぎて見にくい
- **指摘**: 詳細地図で店舗名が多すぎて見にくい。工夫して
- **修正**: 毎フレーム、詳細地図の店名ラベルを画面座標で重なり判定して間引く(`declutterDetailLabels`)。ルート上の店を最優先、次に画面中心に近い順に、既に出したラベルと重ならないものだけ表示(上限48)。接続の「○○へ」と吹き抜けのラベル(extraLabels)は常に出し、その場所を先に占有する。寄れば重ならなくなるので自然に全店が出る
- **技術メモ**: CSS2DObject は `visible=false` で display:none になるので、ラベルの幅は DOM で測らず文字数から見積もる(全角≈10px・半角≈5.5px+余白12px、`.node-label` は translateY(-16px))。画面座標は `getWorldPosition().project(camera)`。CSS2DRenderer は非表示のラベルの要素を DOM から外すことがあるので、テストで `.detail-shop` の総数を数えても意味がない(表示数だけ見る)

## 2026-09-03 ドーチカ⇔アバンザの3本の接続が「明らかにおかしい」
- **指摘**: 3本の接続部分がおかしい。詳細地図の形と地下近辺案内板を見直して直す
- **原因**: 案内板の通路の東(C72〜C84 の間)の桃色部分を「アバンザの地下広場=歩ける床」として一枚の面にしていた。板の色の意味は 桃=区画・歩けない面(アバンザ B1F の高さのデッキ)、白=通路。接続は白い3本(C72 の通路 / C80 の広場=南サンクンガーデン(7段下)とその東縁の曲線階段(17段) / C84 の通路)で、一枚の面にすると3本が消える
- **修正**: 桃色部分は床にせず(建物外形にも入れない)、白い部分(C80 の広場・C84 の通路)をドーチカの床に足す。3本の経路は自エッジの帯(幅5)でアバンザの西壁(OSM 外形)まで引き、中は 広場の東縁(曲線階段の上 j_avz_c)→西壁(avz_c_in)、南は C84 の通路の折れ(avz_s_mid)→館内(j_avz_s)
- **教訓**: 案内板の「桃=施設の色」と「白=通路」を混同しない(リンクスの板と同じ落とし穴)。広場は「その施設の色の面」ではなく通路(白)として床にする。板の建物外形は OSM と 10m ずれることがあるので、館内へ入る点は板の壁ではなく広域のビル外形(OSM)の壁に合わせる(`--apply` の後の NODE_M 上書き)

## 2026-09-03 詳細地図の「○○へ」を地図の外に、店名は消さずにずらして線で結ぶ
- **指摘**: 接続のラベルは地図の上に乗せず必ず地図の外に。店名は重なるなら消さずに文字を離して線で結ぶ
- **仕組み**: CSS2DObject の要素は大きさ0の外枠(`.detail-anchor`)にし、中の span を画面px でずらす(CSS2DRenderer は外枠の transform を毎フレーム上書きするので、外枠自体はずらせない)。引き出し線は `#leaders`(ラベル層の直下の SVG)に1本の path で毎フレーム描く
- **店名**: 区画の重心の真上→上→下→左右→さらに外、の順に候補(labelSlots)を試し、既に置いたラベルと重ならず画面内に収まる最初の所に置く。前回の置き場を先に試す(カメラを動かしたときに跳ね回らない)。真上以外に置いたら線で結ぶ。どこにも置けないときだけ消す
- **接続ラベル**: 矢印(床の縁)から矢印の向きへ画面上で 10px ずつ押し出し、ラベルの箱の角・中央を床の面(y=D.y)に落としてガイド座標で床の中かを判定(`inDetailFloor`)、床にかからない所で止めて矢印と線で結ぶ。文字は地図から離れる側へ伸ばす(span の translate(%) の寄せ)。「床の外」でも通路の網の間に置かれることはある(ホワイティ)

## 2026-09-03 広域の通路をガイドマップ(ベクタPDF)から起こし直した
- **指摘**: 通路はこの PDF(ひとにやさしい地下街ガイドマップ)が正しい。今はめちゃくちゃなので、回転とリサイズで今の地図に合わせて通路を作り直す(ドーチカ以外)
- **PDF の読み方**: pymupdf の `get_drawings()` で塗りの色ごとに層に分ける(白=地下街、桃 (0.97,0.72,0.83)=ホワイティ・ドーチカ、水色=ビル、紫=階段、黄=傾斜、ベージュ=駅)。複合パスはサブパス(始点が前の終点と違う所)で切り、穴は対称差(偶奇)で表す。地下街の網は1本の巨大なパス(外周 561 点+穴 27 個)で、外周だけ描くと巨大な塊に見える(穴も描いて確認する)。白抜きの文字・記号(12pt² 未満)は捨てる。表・凡例・スケールバーの範囲は座標で除く
- **位置合わせ**: 相似変換(縮尺はスケールバー 1.41m/pt、回転 −0.7°)だけでは周辺で 20〜50m ずれる(案内用の略図なので一様な縮尺ではない)。水色ビル ↔ OSM ビルを「重心 45m 以内・面積比 0.5〜2」で自動対応付け(84組)し、重心対応の薄板スプライン(TPS、λ=50)で局所補正すると平均 1m
- **ゾーン×階の割り当て**: 通路の面は一枚の網なので、自グラフの同一階エッジの帯を 10→20→35→60m と広げながら未割当部分を渡す(最寄りエッジの近似)。60m 以内にエッジが無い面は捨てる(未割当 5,800m²)。PDF は階を平面に潰しているので、重なる階のどちらかにしか付かない(もう一方はエッジの帯だけ)
- **落とし穴**: 小さな島(文字の白抜き・階違いの断片)は全ゾーンを出した後に「同じ階の他の面に触れない 150m² 未満」で捨てる。covers は最初の面でなく最大の面に付ける(小片に付くと捨てられない)。同一性判定は再生成したポリゴンでなく元の辞書で(`q is not pg` は常に真)
